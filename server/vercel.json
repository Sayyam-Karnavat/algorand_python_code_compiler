{
    "version": 2,
    "builds": [
        {
            "src": "index.py",
            "use": "@vercel/python",
            "config": {
                "buildCommand": "pip install algokit && export PATH=$PATH:$HOME/.local/bin",
                "pythonVersion": "3.11"
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "index.py"
        }
    ]
}